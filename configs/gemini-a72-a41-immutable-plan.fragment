# A41 immutable evidence/plan/receipt boundary. The exhaustive evaluator and
# architecture-owned mutation implementation remain unavailable, so maxcpus=8
# plus the patch-0092 boot and disable vetoes stay mandatory.
CONFIG_ARM64_MT6797_A72_CAPABILITY_PROFILE=y
CONFIG_LOCALVERSION="-gemini-a41-immutable-blocked"
