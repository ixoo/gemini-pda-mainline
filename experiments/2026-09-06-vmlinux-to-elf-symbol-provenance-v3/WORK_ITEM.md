# Source-forced no-database AArch64 Kallsyms provenance audit

- **Outcome:** perform one fresh retained-image Kallsyms tuple and conservative
  interval audit while proving that neither architecture-signature detection
  nor the installed metadata database can participate. Recover original
  Kallsyms type and one conservative `[start, next distinct symbol)` inspection
  envelope for the four named WLAN lifecycle symbols, or return a bounded
  unresolved result. This can admit a later instruction-analysis contract; it
  does not decode instructions or establish exact ends, calls, returns, xrefs,
  runtime execution, teardown safety or resource quiescence.
- **Parent:** repository commit
  `cd9fbe8e0e8f7eda29b553ae131821d9facd8cf0`. Verify the excluded original
  attempt, the v2 database-gate stop, the RECORD-completeness stop and accepted
  source-only database boundary by the hashes frozen in `inputs.json`. These
  are stop/provenance inputs only. Do not read or reuse the excluded attempt's
  private raw output, bounded metadata, symbol tuples, aliases, neighbors,
  counts or provisional interval verdicts.
- **Direct evidence dependency:** verify the accepted zero-size retained-ELF
  result and its four hashes frozen in `inputs.json`. It establishes why fresh
  original Kallsyms evidence is needed, not any target tuple or interval.
- **Frozen private kernel tuple:** use the sole existing RE-VM workspace:
  Image.gz SHA-256
  `b53d191dc41d3f7364b0fa62b4bc920b1d013a1942b2e6b06727263fc56fcf4d`,
  Image `0570480c28bce1583636a240904df8da3af0b5e5b4bcc6254f5719b42bd723d0`,
  reconstructed ELF
  `cc66df06194d3315335462760962165e1dcb2e50221574aeb45a0805bb17a162`
  and reconstruction diagnostics
  `3e58a3e5c2a43914558f761ccdf4488a9c49b869645b4152ad9b2ee05a2f10b8`.
  Verify all four before content access. Do not reconstruct or copy an ELF,
  export private bytes/logs/databases, or use any prior private parser output.
- **Frozen tool tuple:** installed `vmlinux-to-elf` 1.3.6 under Python 3.12.3;
  launcher `6140be2ee9638573c32b06e5c00ca387e647720b6c8434d123ede119ec11ca6b`,
  script `e3d2de8b5b7ac7ac4e8986369d7a034ae1bbae0af6e08b8d6e6c73c703cb3f15`,
  Kallsyms parser
  `2bff550d9486e90782a4320cec7bc26b249ead5048f58839eec6578b52c06c2d`,
  symbolizer
  `13e79cf4dc37050547f22b65ad35070dceba17473df823c7311311c3fb9e1118`,
  writer `f974b1189155d2cea4773d378990e1b20be866f27719be0593fef3110c56891d`,
  METADATA
  `133b5a6b7fab8081a7c201f2fc63b8a2d7a215475c423c8fe3938aff6a708c43`
  and RECORD
  `ac8d68216a496f0f4dedede5e3ea0d72051bbab7983ab196189da05f5aebdc1f`.
  Record exact hashes before source or private-image inspection. No install,
  update, download, network or database query.
- **Accepted database boundary:** verify the five accepted v2 database record
  hashes in `inputs.json`, including its exact inventory. It conditionally
  permits a future guarded query, but this item deliberately chooses the
  no-database path. The verified database source, SQLite file and excluded
  generated bytecode must not be opened, hashed, imported or queried here.
- **Owner and reviewer:** Astra Medium owns the named loader/parser-state and
  symbol-boundary uncertainty. Sol Medium independently reviews the frozen
  result; `/root` integrates. The owner is not alone in the repository and
  must not revert or edit concurrent files.
- **Model route:** `gemini_specialist`, `gpt-6-astra`, medium for the loader,
  parser and binary-provenance uncertainty; `gemini_reasoner`, `gpt-5.6-sol`,
  medium for cross-file review. No implementation route is selected.
- **Owned scope:** create only this experiment's `README.md`, `inputs.json`,
  `loader.json`, `method.json`, `analysis.json`, `intervals.json`, `FREEZE.md`,
  assert-free normal/optimized verifier and `VALIDATION.md`. Private raw output
  stays in a fresh mode-0700 RE-VM child. Do not edit predecessors, hardware
  facts, support, roadmap, queue, workstreams, workflow ledger, configs,
  manifest, series or patches.
- **Fresh-process source forcing:** use a new Python process with `-B`,
  `PYTHONDONTWRITEBYTECODE=1` and a fresh empty mode-0700
  `PYTHONPYCACHEPREFIX`. Before package execution, install a fail-closed source
  loader for every `vmlinux_to_elf` module. Resolve installed nonsymlink `.py`
  entries only through the pinned distribution RECORD; require a present
  RECORD SHA-256 and size, verify both, compile the exact bytes in memory, set
  the corresponding source origin, and record module name/path relative to the
  distribution, hash and size. Do not call an ordinary loader's `get_code`.
  Refuse package source absent from RECORD, digestless entries, all `.pyc`, zip
  or namespace sources, symlinks, path escape, duplicate origins and more than
  sixteen package source modules. Require every executed package code object
  to come from the recorded loader and the private cache prefix to remain empty.
- **Dependency bootstrap:** before installing the audit hook and source loader,
  preload only standard-library and already installed third-party dependencies
  that a static scan of the admitted package sources proves are required.
  Record module versions and names, but do not inspect dependency source. Stop
  if an unpinned dependency supplies parser semantics, if a dependency is
  missing, or if package import after the gate attempts to discover any new
  bytecode/source outside the admitted distribution. The bootstrap may not
  import any `vmlinux_to_elf` module or access private kernel/database inputs.
- **Fail-closed database stub:** before loading `core/kallsyms.py`, preseed the
  exact module name `vmlinux_to_elf.kernel_db.database` in `sys.modules` with
  precisely the six imported names `KernelVersion`, `KernelRelevantFile`,
  `EMachineValue`, `ArchitectureEMachineLink`, `KnownArchitecture` and
  `DebianRelease`. Each value is a distinct sentinel whose call, attribute,
  subscription, iteration, truth, comparison and representation operations
  record and raise. Ordinary module-name attribute retrieval solely to satisfy
  the import statement is allowed and counted separately; all six sentinels
  must have zero downstream operations. Mark the stub origin as synthetic and
  require that no real `kernel_db` source/data/cache path is opened.
- **Audit guards:** after dependency bootstrap and before package execution,
  reject socket/DNS, subprocess, shell, filesystem writes, every `.pyc` open,
  every open under the real installed `kernel_db` subtree, and unexpected
  reads outside the exact source whitelist plus private Image/ELF identity and
  content reads admitted below. Record zero attempts in each prohibited class.
  Snapshot verified distribution file identities and require unchanged hashes,
  sizes, modes and mtimes afterward, no cache/database sidecars or new package
  files, and an empty cache prefix. A guard hit is a terminal stop.
- **Pre-execution semantic gate:** statically inspect only the source-forced
  package modules required by the Kallsyms path, within the sixteen-module cap.
  Enumerate every assignment/read of parser architecture and database-derived
  state. Prove from exact citations that `extract_db_information()` outputs are
  diagnostic metadata only and cannot change Kallsyms discovery, decompression,
  address calculation, symbol table content/order/type/name or the four bounded
  lookups. Also establish every required frozen architecture field and exact
  value/type convention for ELF64 little-endian AArch64/e_machine 183. Freeze
  this inventory before private Image content access. Stop if either proof
  requires dependency-source expansion, is ambiguous, or identifies a parsing
  dependency on database output.
- **Frozen bypass method:** define one local subclass of the source-forced
  `KallsymsFinder`. It must inherit the original constructor and all parsing
  methods unchanged and override exactly `guess_architecture` and
  `extract_db_information`. The architecture override sets every gate-proven
  field from exact frozen primitives: ELF64, little endian,
  `ArchitectureName.aarch64` and e_machine 183. The metadata override increments
  one counter and otherwise has no effect. Freeze the complete subclass and
  loader/stub source, their SHA-256 values and semantic inventory in
  `method.json`/`loader.json` before private Image content access.
- **Bypass assertions:** before and after the single construction, require the
  exact module/source hash and loader provenance; exact MRO `subclass ->
  source-forced KallsymsFinder -> object`; inherited constructor identity;
  subclass namespace containing no parser overrides except the two reviewed
  methods plus ordinary metadata; both override call counts exactly one;
  unchanged identities for every parsing method used; and exact final
  architecture fields. Temporarily replace `ArchitectureDetector.guess` with a
  fail-closed call-counting sentinel and restore it in `finally`. Detector calls
  and all database-sentinel downstream operations must be zero.
- **Fresh exact-image analysis:** construct only
  `FrozenNoDatabaseAArch64Finder(Image, bit_size=64)` after every gate passes.
  Use its table in memory only to recover exact name, address and original
  Kallsyms type for `do_connectivity_driver_init`, `do_wlan_drv_init`,
  `mtk_wcn_wlan_gen3_init` and `mtk_wcn_wlan_gen3_exit`. Enumerate each target's
  same-address aliases and immediate preceding/next distinct-address symbols.
  Persist only this fresh bounded result and counts. Never compare against or
  import values from the excluded attempt.
- **Interval rule:** admit `[target start, next distinct recovered Kallsyms
  symbol start)` only when exact-name uniqueness, reconstructed-ELF start
  equality, complete same-address alias inventory, strictly greater next
  address in the same executable image region, monotonic tuple ordering and
  noncontradictory type evidence all pass. It is an inspection envelope, never
  an exact end; it may contain padding, literal pools, aliases or tail sharing.
  Classify ordinary/weak/local final linkage only from the fresh original
  Kallsyms type and the pinned type/case transformation, not reconstructed ELF
  `GLOBAL` alone.
- **Bound:** one retained kernel tuple, one installed distribution, at most
  sixteen RECORD-backed package source modules, one frozen loader/stub/subclass,
  one parser construction and four targets. At most four aliases plus one
  previous/next distinct tuple per target. No database path access, dependency
  source inspection, prior raw result, second attempt, whole-table persistence,
  instruction/prologue classification, bytes, disassembly, xrefs, branches or
  calls, reconstruction, network, device or Buildbox.
- **Acceptance predicates:** independently establish all input identities;
  complete source-loader provenance; zero forbidden/open/write/cache activity;
  semantic independence from database outputs; complete architecture state;
  exact frozen loader/stub/subclass and assertions; zero detector calls and
  successful restoration; one fresh unique tuple/strength class per target;
  bounded aliases/neighbors; interval admissibility; and whether envelopes may
  feed a later instruction contract without exact-end claims. A negative result
  is acceptable when it closes the method truthfully.
- **Refusal and validation:** reject identity/source/loader/method drift,
  incomplete RECORD data, ordinary cache loading, unexpected import/open/write,
  dependency source or semantic reliance, database-path access, any sentinel
  operation, unresolved parser state, extra subclass methods, detector call,
  restoration failure, prior-output reuse, multiple construction, ambiguity,
  alias overflow, nonmonotonic/cross-region boundaries, synthesized strength,
  exact-end promotion or semantic instruction claims. Freeze JSON evidence
  before the verifier. Normal and optimized modes need active non-`assert`
  checks plus mutations for every identity, loader, stub, method, sentinel,
  target, alias/boundary, strength, interval and authority class.
- **Rights and privacy:** installed open-source tool text and private retained
  kernel are analysis inputs. Publish only hashes, versions, bounded source
  function/line identities, frozen loader/stub/subclass text, bounded symbol
  metadata/counts, raw-log hashes and independently worded conclusions. No
  private paths, binary bytes, disassembly, long source excerpts, credentials,
  serial/IMEI/calibration data or proprietary content.
- **Hardware/build effects:** none. Device remains known-good Gemian with
  custody released. No SSH/device action, firmware/radio action, build, patch
  integration, commit or push by the specialist.
- **Stop/escalation:** stop on any mismatch, need for scope/source expansion,
  database-derived parser state, guard/sentinel hit, loader ambiguity,
  prior-output dependency, parser ambiguity, alias overflow, decoding need or
  conflict. Return exact evidence, attempts, unresolved question and next
  discriminating check; do not repair by inference.
- **Handoff:** exact parent/dependency/kernel/tool identities; loaded-source and
  dependency inventory; semantic state proof; frozen source/hash records;
  guard/sentinel/restore/construction counts; four fresh neighborhoods;
  per-predicate verdicts; private raw-log hashes; normal/optimized mutation
  counts; limits; review-ready UTC and later-analysis admission.
- **Efficiency loop:** if independently accepted, append one sanitized item to
  the active workflow cohort with actual route/timestamps, first-review result,
  rework/escalation and measured credits or explicit unavailability.
- **State:** frozen for fresh offline specialist dispatch; no device action.
