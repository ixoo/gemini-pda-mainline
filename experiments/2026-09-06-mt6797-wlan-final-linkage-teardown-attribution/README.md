# Retained WLAN linkage: unresolved reconstruction boundary

The retained active Gemian ELF identity matches the frozen input, but this
work stops before disassembly: all four required symbols have zero size in
its reconstructed symbol table. The [analysis](analysis.json) records exact
addresses, bindings, types and the containing executable section. `GLOBAL`
describes this reconstructed ELF; it is not independently established original
linker strength.

The historical input location was absent. The coordinator resolved the sole
retained artifact's current location, and the specialist independently repeated
SHA-256, ELF-header and embedded-release checks. This supersedes the earlier
location blocker in [validation](VALIDATION.md).

No function body was inspected. No whole-executable branch enumeration or
whole-ELF xref scan was run. Call counts and reference inventories are unknown,
not zero; the teardown join remains missing. There is no evidence conflict with
the source audits because their unobserved final-link predicates remain open.

The contract requires exact symbol ranges and immediate refusal on ambiguous
reconstruction. Neither neighboring symbol addresses nor the reconstructed
monolithic WAX section establishes an exact function end. The next
discriminator is an explicitly admitted reconstruction/boundary audit or
original sized-symbol evidence. This requires a clarified contract before
continuing binary inspection.

Raw metadata remains private in the RE VM; only its hash and independently
worded symbol facts are recorded. No binary bytes, disassembly, personal paths,
device access, network lookup, build, firmware/radio action, commit or push are
part of this work. This result establishes neither runtime execution nor safe
teardown or resource quiescence.

The [frozen record](FREEZE.md) and [verifier](verify.py) validate this bounded
unresolved packet. They do not replace the unperformed binary analyses.
