Mainline BMI160 candidate build evidence

Input
-----
- Linux base: `7.1.3` from `kernel/manifest.json`.
- Patch: `v7.1.3/0052-arm64-dts-mediatek-add-disabled-Gemini-BMI160-candidate.patch`.
- Config additions: `CONFIG_IIO=y`, `CONFIG_BMI160_I2C=m`.
- Package: `linux-7.1.3-gemini-bf1f659d1de7`.
- Patchset SHA-256: `bf1f659d1de7adcd641c6867c24b553993f8774fa4f430e4345ebc2c0432665e`.
- Merged config SHA-256: `50eac60404c887d631866ddc05fe021e4a0c4e3020a2458ef48820852ad287a9`.
- Built `Image` SHA-256: `0007b361c982538dca0ae55046b7f5053a963c5abc1eb2e01bc34e3684c390db`.

Checks
------
- `git apply --check` against the prepared source: `PASS`.
- `./scripts/kernel prepare`: `PASS`; all 52 listed patches applied.
- `./scripts/kernel configure`: `PASS`; the merged `.config` retained both
  requested options.
- Focused DTB compile for `mt6797-gemini-pda.dtb`: `PASS`.
- Decompilation of the resulting DTB shows one `bosch,bmi160` node at I2C1
  `0x69`, with the recovered direction-7 mount matrix and `status =
  "disabled"`.
- Focused compilation of `drivers/iio/imu/bmi160/bmi160_core.o` and
  `bmi160_i2c.o`: `PASS`.
- Complete `./scripts/kernel build`: `PASS`; the package contains the kernel
  image, DTBs, merged config, System.map, and patch provenance.

Schema note
-----------
The direct `dt-validate` invocation against the Linux 7.1.3
`bosch,bmi160.yaml` binding reports a false-schema error for this node. The
same command reports the identical error for the existing upstream
`qcom/msm8916-longcheer-l8910.dtb` BMI160 node, so this is not promoted as a
Gemini-specific binding regression. The binding document itself remains the
upstream reference; investigate the 7.1.3 schema/tool version mismatch before
claiming a clean `dtbs_check` result.

Safety boundary
---------------
The node remains disabled and contains no guessed supplies or interrupt. The
build proves syntax, binding shape, and driver compilation only; it does not
prove chip identity, rail availability, I2C operation, or runtime IIO support.
