# Validation record

Executed on 2026-09-06 after the one fresh prospectively admitted batch.

- Normal and optimized verifier: PASS, 77 refusals each; six predicates,
  nineteen binary anchors and six static-child receipts.
- Independent scope fixtures bypass complete hashes and reject second batch,
  replay, extra tool/interval, expanded code, controller/count/budget drift,
  external input/evidence, diagnostics, symbol-server configuration and forbidden
  debug/DWARF options. Separate predecessor comparison rejects tuple mutation.
- Further refusals cover binary/co-mutated identity, request/call/value-origin
  invention, pointer/scalar confusion, command order, hidden propagation,
  retry/gating, runtime/mainline promotion and missing receipt/no-hit records.
- All eight files: JSON parsing, in-memory Python compile, newline/whitespace,
  local links, retained-suffix allowlist, bounded privacy/instruction-listing
  patterns and no-pycache checks PASS.
- `git diff --check`: PASS; a separate all-file whitespace check includes
  the untracked experiment files omitted by that Git check alone.
- `./scripts/check-repository`: PASS (`repository_checks=pass`), including
  195 manifest profiles, source-integrity/publication/target guards, backend
  and preflight fixtures, host tools and workflow validation. Existing metadata
  debt remains 37 records. Printed Linux-only and full-package skips/deferments
  were not counted as passes.

Privacy/rights review: only exact selected device/key literals, address and
receipt metadata, and independently authored semantics/verifier code remain.
No raw bytes, instruction listing, function dump, decompiler output, strings
corpus, database, private path, credential or personal identifier is retained.
There is no implied source/binary redistribution or vendor ABI reuse right.
No temporary files or copied binary were created.

No shell was changed, so bash syntax and ShellCheck are inapplicable. No kernel
change, build, checkpatch, DT-schema, device/SSH, live process, network,
execution/emulation, ioctl, Buildbox, staging, commit or push occurred. The
approved RE VM performed static reads only. There was one disassembly, no
replay and no selected interval outside the frozen contract. All six static
children completed without diagnostics; separate host-launcher capability
warnings are explicitly recorded in analysis.json. No v3 repair was needed.

Unresolved risks: actual runtime branch/value/effects; libc parse and ioctl
conversion; startup caller/process result; resource lifetime and firmware/radio
behavior; and the absent standard mainline interface design. The kernel's
arithmetic aggregate cannot prove individual component success. No hardware
support is claimed, and no remaining analysis budget permits another probe.

The coordinator owns independent review, any accepted/excluded pilot-03 record
and publication checks. Credits remain unavailable.
