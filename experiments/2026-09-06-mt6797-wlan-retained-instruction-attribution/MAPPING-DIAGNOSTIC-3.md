# Nonreentrant mapping diagnostic: import-budget refusal

The single [Amendment 10](AMENDMENT-10.md) diagnostic stopped at the fixed
256-import-event cap. It did not complete the pyelftools import or identify the
previous mapping delta. No native/mapping exception is granted and the private
analysis budget remains unused.

## Frozen inputs and chronology

- Dispatch: `b01c052bd288acae0b833588e1d387b32e303873`; Astra Medium.
- All 30 controlling and 18 dependency hashes passed before the run.
- A parent-supplied retained-boundary path initially mismatched its pin. Work
  stopped without edits or execution; the parent withdrew that path and supplied
  `../2026-09-06-mt6797-wlan-final-linkage-teardown-attribution/`. Its four exact
  pins passed on the resumed dispatch; no pin or dependency content changed.
- [Frozen source](mapping-diagnostic-v3.json), frozen
  `2026-09-07 03:46:25 UTC`, file SHA-256
  `d90eda1a2304668f237389f56345837dba3523db406c1827217f0fcaccbffaf9`;
  embedded source SHA-256
  `fa8d422a4c23b768e4e05315fab6845cb5174a7ebd208244c915be88e43a0d9e`.
- One fresh `/usr/bin/python3.12 -I -S -B` invocation through the approved
  RE-VM shell, from `2026-09-07T03:47:04.598332+00:00` to
  `2026-09-07T03:47:23.813494+00:00`; no rerun or repair.
- [Result receipt](mapping-diagnostic-result-v3.json), SHA-256
  `d7603f480a4bec276c6ecc123de63fa3674f2c08ef8ce9073c87c588b8b1174e`.

## Observation and limits

The warm proof passed: identical file-backed dictionary and full pseudo rows,
unchanged module objects, unchanged native/prohibited counters, and zero
observer import audit events. The frozen observer baseline contained 194 module
objects. The receipt records 512 completed observer intervals with zero observer
import audit events, and restoration of the exact original import object.
These counts make no claim about cached import calls without an audit event.

The terminal result is `RuntimeError: import event cap`, with 256 recorded
import events, 55 entered mapped modules and 53 completed mapped modules. It
counted one affected ordinary file and four affected mapping rows. The refusal
serializer preserved only those aggregate counts, not the affected identity,
first-observer event, native-audit list or static source join. Consequently no
particular extension, file or causal import route is established. The required
73 completed modules and final closure/restoration checks were not reached.

The receipt records one admitted Capstone native request, zero optional
requests, and zero write, cache-write, network, process and private-read events.
No engine, method, callback, ELF analysis, device operation or build occurred.
No raw maps, virtual addresses or private paths were retained or exported.
The RE shell was closed immediately after the refusal.

## Validation and handoff

Tests actually run: exact dependency SHA-256 comparisons; host source-digest
verification and Python syntax compilation before execution; the one guarded
RE-VM diagnostic; result/source identity and refusal-predicate checks; and
`git diff --check`. No hardware test or kernel build was applicable.

Stop here. The unresolved question remains the exact post-Capstone mapping
component and first-observing import route. A prospective reviewed diagnostic
change would need to preserve bounded partial observations on refusal and
justify any import-budget change before another run. Neither the counter nor
the budget was changed after this result. Earlier artifacts remain immutable.
