# Site-disabled source-forced decoder startup amendment

This prospective amendment is controlling contract evidence and resolves only
the tool-preflight stop in [README.md](README.md). Its exact SHA-256 and the
amended [WORK_ITEM.md](WORK_ITEM.md) SHA-256 must be added to and verified from
[inputs.json](inputs.json) before any private-content access. The verifier must
reject drift in either file. Every dependency, private-input, instruction,
scan, bound, privacy and refusal rule remains in force. The single private
analysis execution remains unused.

## Refusal evidence

At dispatch `53d45455d76258c7d8fb06aa2cf619f88ace0ac3`, all eighteen
dependency hashes passed after exact path clarification. Read-only tool
introspection found the installed versions, but normal interpreter startup had
already loaded a symlinked system `sitecustomize.py`. The specialist stopped
before completing the component inventory, freezing an analysis method,
opening the private ELF or decoding an instruction. That normal startup and
all of its component observations are excluded from result evidence.

## Frozen source-only bootstrap

Before any new package import, create `bootstrap.json` containing the complete
independently written isolated-startup, RECORD parser, source-forcing loader,
native-load guard, optional-module refusal and component-inventory source plus
its SHA-256. Freeze that file before executing the bootstrap. Syntax-only
compilation is permitted. The final verifier must pin `bootstrap.json`, its
embedded source hash and its pre-execution chronology.

Run the exact installed Python 3.12.3 interpreter for the fresh tool preflight
and later analysis child with `-I -S -B`. Before decoder import, require
`site`, `sitecustomize`, `usercustomize`, `ccapstone` and every `capstone` or
`elftools` module absent from `sys.modules`. Record the resolved nonsymlink
interpreter executable hash, size, mode, mtime, version and isolated flags.
The symlinked startup hook must remain unloaded and is neither admitted nor a
selected component.

Use isolated standard-library `sysconfig` and `importlib.metadata` without
importing `site` to locate exactly installed Capstone 4.0.2 and pyelftools 0.30
distribution roots and RECORD files. Every selected package `.py` must be an
absolute nonsymlink regular file beneath its exact distribution root with a
present RECORD SHA-256 and size that both match. Install a terminal prefix
finder that compiles those exact verified source bytes in memory and refuses
every package source absent from its explicit map, all `.pyc`, zip/egg,
namespace extension, path escape, duplicate origin and ordinary finder
fallback. Record every executed package module and source-derived code object;
the cap is 64 modules. No decoder package bytecode is admitted, so cache/source
pairing is not used. `-B` and the audit must prove zero cache writes.

## Exact optional and native-load routes

Before decoder import, statically inspect only the exact RECORD-backed Capstone
binding sources needed to identify its optional `ccapstone` import and native
library callsite. Freeze the exact source citations, called Python object and
fallback behavior. Install a terminal meta-path refusal for module name
`ccapstone`: each exact import lookup is counted and raises `ImportError`, no
module object is created, and no optional code/file discovery is allowed.
Static source must prove that this refusal selects the ordinary ctypes path.
Any lookup other than the exact import protocol, successful optional import,
downstream optional-module use or request count drift is a stop.

Resolve one exact existing nonsymlink native Capstone library before package
import and freeze its absolute path, hash, size, mode, mtime and native version.
From the statically proved binding callsite, identify and wrap the actual
callable endpoint or endpoints used for the bare `libcapstone.so.4` request,
including an already-created `ctypes.cdll` loader if applicable. Require
exactly one bare-name request, substitute only the frozen absolute path, record
exactly one successful primary load and reject every other post-wrapper native
load request. No ordinary library search fallback is permitted.

Snapshot `/proc/self/maps` immediately before and after decoder import. Freeze
the absolute path, hash, size, mode and mtime of every mapped native file in the
baseline and every newly mapped file; reject deleted/unresolved/symlinked,
anonymous executable or relative mappings and more than 128 native components.
Independently parse the primary library's ELF dynamic table with the frozen
standard-library-only parser. Record each `DT_NEEDED` name and require its
resolved exact file either in the frozen baseline mapping or newly mapped
inventory. Reject an unaccounted dependency, mapping drift or additional
native component. This inventory, not an assumed single `.so`, defines the
native decoder closure.

## Two-phase freeze and one private analysis

The isolated preflight process imports and inventories tools without opening
the private ELF, then exits. Use its sanitized receipt to create and freeze
`method.json` containing the pinned `bootstrap.json` identity, complete tool
and native inventories, static load-route proof, full analysis harness/ELF
mapper/raw decoder/traversal source and every source hash. This is the final
method freeze before private content.

Launch one fresh `-I -S -B` analysis child using the already frozen bootstrap
and method. It must reproduce every source/native/interpreter identity and
load-route count before opening the exact private ELF. Install the irreversible
Python audit hook before private reads; it remains active until child exit.
At the tool-import boundary require the actual native callable endpoint(s)
restored by exact object identity, the `ccapstone` finder removed by exact
identity with `ccapstone` still absent, and `sys.path` restored to its exact
isolated-startup value. No decoder import or plugin discovery is permitted
after that boundary. Process exit is the boundary for all already loaded
modules; the audit hook is never claimed removable or restored.

After the one private analysis, require every frozen interpreter/package/
source/native file unchanged in hash, size, mode and mtime; zero filesystem or
cache write attempts; identical package code-object provenance; exact native
mapping closure; continued absence of all site customization/optional modules;
and the named endpoint/finder/path postconditions. The outer collector may
write only its two already admitted private pipe captures.

## Additional refusals

Refuse amendment or amended-contract drift; bootstrap not frozen before first
package import; non-isolated flags; any site/environment/user-site influence;
unresolved/symlinked or out-of-root interpreter/package/native component;
decoder bytecode or ordinary finder participation; optional-module success;
unproved/uncounted native load route; request/substitution count drift;
ordinary library search; unaccounted `DT_NEEDED` or mapped file; component cap
overflow; named restoration failure; removed/claimed-restored audit hook; or
private ELF access before the complete method freeze. Do not use the excluded
normal-startup observations as the fresh inventory and do not run a second
private analysis.
