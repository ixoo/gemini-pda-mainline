# Pre-entry inventory chronology clarification

This thirteenth prospective amendment resolves only an ordering ambiguity
between [AMENDMENT-7.md](AMENDMENT-7.md) and
[AMENDMENT-12.md](AMENDMENT-12.md). No v7 bootstrap was created or executed,
and no private content was read before this clarification. All earlier frozen
artifacts and results remain immutable.

Amendment 7's complete-map gate is unchanged. Before entering any Capstone or
pyelftools source module, discover, identity-freeze and verify both complete
installed source-forced inventories: exactly 25 Capstone source modules and 52
pyelftools source modules, for the exact combined 77-module budget. Refuse any
entry into either package before both inventories and every pre-entry identity,
containment, uniqueness and inert-asset predicate have passed.

After that common pre-entry gate, sequence execution exactly as follows:

1. execute the already inventoried Capstone imports and verify its exact native
   library and dynamic closure;
2. snapshot the resulting interpreter/stdlib/Capstone mappings, then perform
   Amendment 12's exact `resource` spec, identity, ELF, `DT_NEEDED`, audit,
   module-object and four-row mapping verification once under owner
   `stdlib-resource`; and
3. execute the already inventoried pyelftools imports, requiring its frozen
   source request for `resource` to reuse the identical cached module object
   without rediscovery, reinitialization, native-load request or mapping delta.

Amendment 12's phrase “Then run the complete source-forced pyelftools
inventory/import” therefore means execute the complete source-forced import
from the inventory already frozen at the common pre-entry gate. It does not
authorize a late, repeated or mutated inventory. Package discovery remains
closed after the common gate.

Before replacement preflight, pin and verify in [inputs.json](inputs.json) the
exact SHA-256 values of this amendment and the amended
[WORK_ITEM.md](WORK_ITEM.md), in addition to every existing controlling and
dependency hash. Reject drift.

The replacement remains `bootstrap-v7.json`: exactly one no-ELF preflight, then
only on complete success the already admitted single analysis-mode execution.
All Amendment 8 and Amendment 12 lifetime, callback, mapping, restoration,
privacy, authority and refusal predicates remain unchanged. No package entry,
bootstrap execution, private read, network, device action or build is admitted
by this clarification itself.
