# Original caller-frame handoff amendment

This fifteenth prospective amendment resolves only the wrapper-frame defect in
[RESOURCE-CALLSITE-DIAGNOSTIC.md](RESOURCE-CALLSITE-DIAGNOSTIC.md). The v1
diagnostic source, result and refusal record remain immutable. Its static scan
found one candidate, `resource.getpagesize` in exact frozen
`elftools.elf.elffile` source at line 17, but its generic guard refusal occurred
before cached resource reuse, profile installation or a runtime callsite join.
No function, engine, callback, method or private read was admitted; the single
private analysis remains unused.

Before another diagnostic, pin and verify in [inputs.json](inputs.json) the
exact SHA-256 values of this amendment, the amended
[WORK_ITEM.md](WORK_ITEM.md), all earlier controlling artifacts and the complete
v1 callsite-diagnostic trio. Reject drift.

## Exact direct-caller handoff

Create and freeze `resource-callsite-diagnostic-v2.json` with the complete
independently written source and embedded SHA-256 before first execution. Run
it exactly once with `/usr/bin/python3.12 -I -S -B` through the approved RE-VM
shell. Reproduce every Amendment 14 identity, inventory, static-candidate,
resource-preload, pre-call terminal, audit, bound, privacy and seven-restoration
predicate without weakening or expanding them.

Change only the cached-resource wrapper join. At entry to the process-local
terminal import wrapper, before calling another helper or the original import,
read its direct caller frame once. For the sole exact level-zero `resource`
request under owner `pyelftools`, require the caller globals' `__name__`, code
filename and current line to equal the frozen `elftools.elf.elffile` module,
exact canonical source path and line 16 import statement. Immediately reduce
that observation to an immutable bounded tuple of module name, canonical
filename and line; do not retain the frame, globals, locals or code object.

Pass that explicit tuple to the cached-resource helper. The helper must validate
the tuple against the exact frozen source identity and unique line-16 AST import
join instead of inspecting its own immediate caller. It must still require the
original import object, identical cached module object, unchanged module state,
zero loader reinitialization and exactly one cached request. Refuse any second
resource request, nonempty fromlist, nonzero level, different owner, caller,
file or line, mutable/unbounded tuple, or attempt to infer a caller through a
fixed stack depth inside a nested helper.

Install Amendment 14's exact initially-`None` profile hook before the top-level
pyelftools import, unchanged from v1. After cached reuse, continue only until
its first validated `resource` `c_call`. Keep the same static candidate
inventory, exact callable-object/source/frame/line join, canonical
`first_callsite`, pre-call payload-free terminal signal, profile auto-unset
proof, active-boundary propagation, exact handler and seven restorations. The
sole sequencing change is the explicit original-frame handoff needed to reach
that observer.

Write only `resource-callsite-diagnostic-result-v2.json` and
`RESOURCE-CALLSITE-DIAGNOSTIC-2.md`, with the same sanitized fields and decision
boundary as Amendment 14. Stop after the one diagnostic. A successful join may
identify a callable but still does not admit its execution, return value,
effects, pyelftools usability, engine construction, method, callback or private
analysis. Any further decision requires a later prospective reviewed amendment
grounded in the callable's documented semantics and exact frozen caller use.
No acquisition, network, device action or build is admitted.
