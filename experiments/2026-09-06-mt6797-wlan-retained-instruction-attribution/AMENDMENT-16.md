# V1 profile-installation evidence correction

This sixteenth prospective amendment corrects one historical phrase in
[AMENDMENT-15.md](AMENDMENT-15.md) before any v2 construction or execution.
Every frozen artifact and result remains immutable. No private content was
read, and the single private analysis remains unused.

Amendment 15's opening says the v1 diagnostic refused before profile
installation. That statement is withdrawn. The immutable v1 source installed
its dedicated profile function before starting the top-level pyelftools import,
as Amendment 14 required. The refusal occurred after package entry but before
cached resource reuse, a matching runtime `resource` `c_call`, terminal latch or
exact-signal handler. The receipt's false `profile_auto_unset` field means only
that the matching terminal profile-event/handler path was not established; it
does not prove the profile function was never installed.

The operative Amendment 15 correction is unchanged: keep the Amendment 14
pre-import profile installation and pass the exact direct package-caller tuple
from the terminal import wrapper to the cached-resource helper. Do not infer a
new chronology or alter any inventory, static candidate, callable join, latch,
auto-unset, active-boundary, handler, restoration, privacy or authority rule.

Before v2, pin and verify in [inputs.json](inputs.json) the exact SHA-256 values
of this amendment and the amended [WORK_ITEM.md](WORK_ITEM.md), in addition to
every existing controlling and dependency hash. Reject drift. This correction
admits no diagnostic execution, function call, engine, method, callback,
private read, network, device action or build.
