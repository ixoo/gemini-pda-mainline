# Gemini DA921x, I2C6, and Cortex-A72 boundary

This document records the durable hardware conclusions at the boundary between
MT6797 I2C6, the Gemini's external DA921x-family buck regulator, and the two
Cortex-A72 CPUs. Detailed candidate construction, raw observations, hashes, and
failed branches remain in the linked experiment records.

## Scope

| Field | Value |
| --- | --- |
| Device | Named Gemini PDA development unit |
| Last verified | 2026-08-05 |
| Kernel baseline | Linux 7.1.3 plus the repository patch/profile system |
| Decisive read-only evidence | [Post-event identification lifecycle](../../experiments/2026-08-01-da921x-post-event-lifecycle/README.md), [first natural CPU8 owner cycle](../../experiments/2026-08-02-gemian-a72-first-cycle-latch/README.md), [repeatable bounded scheduler-context execution](../../experiments/2026-08-03-a72-scheduler-context/results/runtime-unpark-attempt-2-repeatability-pass-20260805.txt), the [safe-off ownership contract](../../experiments/2026-08-05-a72-safe-off-ownership-contract/README.md), and the [secure CPU-off attribution audit](../../experiments/2026-08-05-a72-secure-cpu-off-attribution/README.md) |
| Safety position | An experiment-only Gemian-derived line repeatably retains both A72 CPUs and completes bounded per-CPU scheduler-context work. Static secure attribution now distinguishes retained-CPU9 and last-A72 effects, but the corrected active-`AFFINITY_INFO` contract remains blocked, so default/mainline CPU8 and CPU9 remain disabled pending membership/provider ledger, notifier and hotplug/suspend admission, bounded readback, provider, and runtime ownership. |

## Current boundary

| Area | Current conclusion | Confidence | What remains |
| --- | --- | --- | --- |
| I2C6 ownership | The DVFSP handoff can leave I2C6 serviceable without taking away the shared AP-DMA clock from the working I2C5 path. | `observed` | Resume, error recovery, and stress are untested. |
| Native short transfer | The native packed/FIFO path completes the exact one-byte pointer plus one-byte read transaction. | `confirmed` for this exact shape | Arbitrary lengths, plain writes, and other combined-transfer shapes are unproved. |
| Board-control register contract | The named board exposes a stable legacy DA9213/DA9214/DA9215-compatible direct-address tuple at `0x68` and `0x69`. | `confirmed` for one invocation on one unit | This does not uniquely distinguish DA9213, DA9214, and DA9215. |
| Linux regulator driver | The upstream DA9211/A-family probe does not match the observed legacy protocol. The dedicated local legacy-family identification driver binds naturally, exposes no provider, and passed one unbind/rebind lifecycle. | `observed` on one unit | Provider constraints, ownership, rollback, resume, and writable operations remain missing. |
| Real-compatible OF integration | The real-compatible client traverses its natural event path and binds the identification driver. Natural bind plus one rebind completed exact `14 -> 14 -> 28` reads with every write/other counter zero; primary and page-2 ownership persisted after a bounded page-2 sysfs-visibility delay. | `observed` on one unit | Repeatability across cold boots and units is unproved; provider work remains separate. |
| Cortex-A53 cluster | Logical CPUs 0–7 can remain online together on the current serviceability baseline. | `observed` | Coherency stress, cpufreq, idle, suspend, and thermal validation remain open. |
| Cortex-A72 cluster | An exact experiment-only Gemian-derived line brings both A72 CPUs online while retaining the cluster and fail-closed public CPU-down veto. Bounded shared-memory/coherency load and one normal-priority bound task per CPU complete repeatably through ordered scheduler-context cleanup and attributable watchdog recovery. Static secure attribution establishes that `CPU_OFF` parks the target and `AFFINITY_INFO` actively performs teardown: retained-CPU9 off has exact target diagnostic/private-ledger effects but no cluster-power effects, while last-CPU8 off enters CCI, cluster/SPM, B-mux/PLL, and external-isolation teardown. | `confirmed` for repeatable bounded retained CPU8/CPU9 execution and scheduler-context cleanup; `confirmed` as a static callgraph attribution for the exact private payload; stable/default support `unknown` | Secure waits are unbounded. Membership/provider ledger, notifier and policy/suspend admission, independent readbacks, SRAM/DCM and provider ownership, CPU_OFF runtime, sustained/userspace load, resume, OPP/cpufreq, thermal policy, and default integration remain open. |

## Exact durable observations

One gated invocation completed 14 of 14 fixed native transfers. Both
passes over secondary address `0x69` returned:

```text
register 0x05 -> 0xd9
register 0x06 -> 0xd0
register 0x47 -> 0xc0
```

Both passes over primary address `0x68` returned:

```text
register 0xd3 -> 0x1f
register 0x5e -> 0x00
register 0xd9 -> 0x46
register 0xda -> 0x46
```

Distinct receive prefills were overwritten. Each transfer used packed length
`0x0101`, auxiliary length zero, the FIFO receive path, a completion interrupt,
and a FIFO count that changed from one to zero. AP-DMA did not run. The
diagnostic counters changed from `0,0,0,0` to `14,0,14,14`; initialization
remained `1,1`, and the temporary retry-policy change was restored. There was
no explicit reset, error reset, fatal signature, or I2C timeout.

CPU0–7 availability, the CPU8/9-offline state, DVFSP handoff, boot identity,
and USB serviceability remained stable across the invocation. This is repeated
evidence inside one invocation on one unit, not cross-boot or cross-unit
repeatability.

The built-in identification lifecycle independently observed natural bind at
`14/8/6`, zero-transaction unbind, and one rebind reaching `28/16/12`.
DMA-start and every write-only, register-data-write, other-shape, and
other-address counter stayed zero. The primary driver link, page-2 client and
exact I2C `dummy` driver, two identity logs, read-only sysfs, and CPU/USB/console
serviceability persisted. Page-2 sysfs visibility lagged the synchronous bind
return, so the reproducible checker uses bounded read-only polling. This is one
lifecycle on one boot, not a cross-boot stability claim.

An exact identity-gated Gemian observer independently latched the first natural
CPU8 cycle before userspace retrieval. Its two delayed proc reads were
byte-identical at `frozen-complete`, with 46 records, no overflow, CPU8-up
transaction 2 and CPU8-down transaction 3. The successful up path took
6,008,000 ns and observed DA9214 BUCKB `0 -> 1`, exact SPM reset/isolation
readbacks, TOPRGU assert/deassert, matching raw and mapped PSCI identities,
secondary-online publication, DCM enable, stable secure sentinels, and
immediate clock snapshots. The later 5,193,154 ns down path observed CPU8 leave
the online mask, affinity handling, BUCKB `1 -> 0`, DCM disable, restored SPM
reset/isolation state, and a successful HPS-down end. No synthetic load or
state-changing diagnostic write ran. This is one vendor-path cycle on one unit,
not mainline provider, rollback, resume, CPU9, or repeatability evidence.

A later one-shot failure discriminator established the narrower rollback
boundary with high confidence on the same unit: Linux can enable and settle
BUCKB, stop before external-isolation clear, and restore its exact owned BUCKB,
SPM-reset, and TOPRGU pre-state. It does not establish an inverse after
external isolation. Public Linux contains no isolation restore in its A72 off
path, while the natural-cycle final restore occurs outside the instrumented
Linux writer, so that later owner remains unresolved rather than assigned to
Linux. See the [accepted rollback record](../../experiments/2026-08-02-a72-pre-isolation-rollback-discriminator/results/runtime-2-20260802.txt)
and [isolation-owner audit](../../experiments/2026-08-02-a72-one-way-cpu8-boundary/results/isolation-owner-audit-20260802.txt).

The subsequent one-way candidate crossed that boundary once. Retained ramoops
contains nine ordered checkpoints from SPM reset through DCM and exactly one
terminal `cpu8-online-held` marker. The marker's source gate requires PSCI
success, generic secondary completion, CPU8 online, CPU9 absent, and verified
DCM readback. Vendor logs then reported cluster 2 on at 845 MHz. About 1.17
seconds later HPS entered generic CPU-down; the pre-platform cpuhvfs notifier
faulted before the platform CPU-disable veto. This establishes CPU8 startup
feasibility but not stable execution or safe offlining. See the
[sanitized runtime result](../../experiments/2026-08-02-a72-one-way-cpu8-boundary/results/runtime-attempt-1-cpu8-online-20260802.txt).

The fail-closed held-online child then prevented HPS and public CPU8/9 down
requests from reaching the notifier path. Two exact changed-cycle runs each
reached a late synchronous CPU8 callback with CPU8 accounted online, CPU9
offline, and cumulative callback count three at 12.415481 and 12.265514
seconds. Both runs had zero retained fault/down-veto/panic records, returned
through the fixed watchdog to known-good Gemian, and preserved the exact boot2
image. This establishes repeatable bounded CPU8 execution/accounting on the
named unit, not safe CPU_OFF, general or sustained load, CPU9 execution, DVFS,
thermal, or suspend behavior. This CPU8-only result does not itself establish
CPU9 execution or scheduler-context execution. See the [repeatability result](../../experiments/2026-08-03-a72-cpu8-late-hold/results/runtime-attempt-2-repeatability-pass-20260803.txt).

A sanitized static audit of the exact verified private secure payload now
attributes the off-side split. Target `CPU_OFF` follows the generic TF-A v1.1
path into WFI; a controlling CPU's `AFFINITY_INFO` request then actively invokes
the A72 hardware teardown. With CPU8 retained, the decision-relevant CPU9 path
writes the diagnostic monitor, target-core power control, and private secure
membership state, but does not enter CCI, cluster-top, SPM, B-mux, or B-PLL
teardown. When CPU8 is last, the zero-membership branch withdraws CCI and
executes cluster/SPM and B-mux/PLL shutdown, including setting SPM external-
isolation bit 1 at `0x10006290`. That callgraph contains unbounded WFI and
acknowledgement waits and no direct MP2 DCM or SRAM-LDO write. An
`AFFINITY_INFO` query for retained online CPU8 is not an observation: while its
private membership bit remains set, it enters CPU8 teardown and can wait for
CPU8 WFI indefinitely. This is exact static ownership evidence, not CPU_OFF
runtime completion or an independent hardware readback. See the
[secure CPU-off attribution audit](../../experiments/2026-08-05-a72-secure-cpu-off-attribution/README.md).

## Observation and inference boundary

The owner later reported that some white/grey-screen cycles followed by an
apparent automatic reboot may have been reboot-path or boot-selection issues.
Those visual observations and returns to Gemian remain part of the experiment
chronology, but without an exact runtime identity, durable stage record, or
attributable crash/reset evidence they are inconclusive about whether the
candidate kernel failed. The positive identity-gated results below remain
direct observations; contrasts that depended only on an earlier visual/reboot
cycle do not establish a kernel-causal failure boundary.

The directly observed bytes, transaction shape, and preserved serviceability
support a high-confidence inference that this board uses the legacy
DA9213/DA9214/DA9215 control contract. The independent Gemian and binary
evidence specifically favors the documented DA9214 board design, but the
read-only tuple is not a unique silicon identifier.

The diagnostic requested pointer bytes only. It did not issue a register-data
write, change `PAGE_CON`, register a regulator provider, select a voltage,
enable a rail, or request an A72. It is therefore accurate to say that the
diagnostic requested no voltage or enable action. It is not accurate to claim
that the rail was independently measured unchanged.

Independent serviceability discriminators also establish that generic I2C
client creation, the fixed resource contract, the compatible-derived client
name, and the I2C modalias are independently serviceable in their bounded
tests. They do not identify the cause of an ambiguous earlier reboot. A
name-only `da9214-legacy` client remained
unbound and serviceable with no OF node and zero I2C activity. An unmatched
compatible OF client also remained unbound and serviceable. The remaining
software integration boundary is transport of the completed real-compatible
I2C device event. Bounded runtime classification proved that the event contains
eight ordered fixed entries, including the OF modalias, followed by one final
`SEQNUM`; the I2C fallback modalias is absent. A corrected validator reached
final stage 17 and normal cleanup with transport suppressed, while the unbound
client, USB serviceability, CPU0--7, handoff, and every zero-I2C-activity gate
passed. The next selected boot reached stage 18 only after the normal allocator
produced the exact 293-byte skb and its header, environment, credentials,
destination group, and port ID all matched; the skb was consumed before socket
traversal or multicast and the same baseline passed. This does not identify new
hardware behavior. The following selected boot retained that exact baseline,
reached stage 19 after normal mutex-protected socket-list traversal, and
observed one socket entry with zero group-1 listeners while still consuming the
skb before multicast. The count is a bounded observation of that initramfs
boot, not a general property of Linux userspace.

The read-only board-contract gate is closed. The following are not established:

- general I2C6 correctness across arbitrary messages, failures, reset recovery,
  suspend, or stress;
- page-selection semantics or any two-byte/register-data write;
- which buck output supplies the A72 cluster and who currently owns it;
- voltage selectors, current limits, enable state, constraints, IRQ behavior,
  rollback, or resume behavior;
- safe provider registration with the existing firmware state;
- a bounded failure/rollback transaction at or beyond external-isolation clear;
- a locked membership/provider ledger, early hotplug and suspend admission,
  notifier exclusion, and bounded independent CPU9-off/last-user readbacks;
- a mainline representation of the observed SRAM-LDO, SPM, clock, CCI,
  coherency, regulator, and firmware sequence;
- longer or sustained CPU8/CPU9 execution, safe CPU8/CPU9 offlining,
  cpufreq/OPP, idle, suspend, thermal, or general scheduling policy.

## Implementation decision

Do not make the upstream DA9211/A-family driver emulate the legacy vendor ABI.
The observed chip family, register protocol, transport contract, and resource
model require either:

1. a dedicated DA9213/DA9214/DA9215 legacy-family driver and binding; or
2. a strictly separated legacy variant in an appropriate family driver, with
   no shared probe path that performs A-family identification writes.

The first implementation must use fixed direct addresses and a fail-closed
read-only match. It must not change `PAGE_CON`, write a regulator register,
register a writable consumer path, or request an A72.

The first-stage contract from the
[legacy-family driver design](../../experiments/2026-07-29-da921x-legacy-driver-contract/DESIGN.md)
is implemented locally as an identification-only driver. Its exact
14-transfer probe, zero-transaction unbind, and read-only rebind have runtime
evidence. It deliberately exposes no provider and does not establish shutdown,
suspend/resume, rail ownership, rollback, or any writable operation.

## Safety invariants

The authoritative ordered sequence is the
[roadmap](../ROADMAP.md#ordered-gates). This hardware boundary constrains every
step without prescribing their order:

- profiles rejected by the active profile-series audit are not valid
  foundations;
- require fixed-address, fail-closed matching and zero probe-time
  register-data writes;
- keep all regulator consumers and CPUs 8–9 disconnected until their explicit
  gates;
- preserve console, keyboard, USB, CPU0–7, DVFSP, I2C5/AP-DMA ownership, and
  reboot at every hardware step;
- no bounded write is permitted without established ownership, a no-op,
  readback, rollback, and resume behavior; and
- treat CPU8, CPU9, coherency, OPP/cpufreq, idle, suspend, thermal, and
  scheduler policy as separate acceptance gates.

Any failure before a trustworthy register observation remains an I2C,
ownership, or serviceability failure—not regulator or A72 evidence.

## Evidence index

- [Secure CPU-off attribution audit](../../experiments/2026-08-05-a72-secure-cpu-off-attribution/README.md)
- [Safe-off ownership contract](../../experiments/2026-08-05-a72-safe-off-ownership-contract/README.md)
- [A72 ownership and rollback audit](../../experiments/2026-08-02-a72-ownership-rollback-audit/README.md)
  and [machine-checked boundary matrix](../../experiments/2026-08-02-a72-ownership-rollback-audit/results/ownership-matrix.tsv)
- [First natural CPU8 owner-cycle latch](../../experiments/2026-08-02-gemian-a72-first-cycle-latch/README.md)
  and [sanitized exact snapshot](../../experiments/2026-08-02-gemian-a72-first-cycle-latch/results/runtime-first-natural-pair-20260802.txt)
- [One-way CPU8 online checkpoint](../../experiments/2026-08-02-a72-one-way-cpu8-boundary/README.md)
  and [sanitized runtime result](../../experiments/2026-08-02-a72-one-way-cpu8-boundary/results/runtime-attempt-1-cpu8-online-20260802.txt)
- [Late CPU8 execution/accounting hold](../../experiments/2026-08-03-a72-cpu8-late-hold/README.md)
  with [first runtime pass](../../experiments/2026-08-03-a72-cpu8-late-hold/results/runtime-attempt-1-pass-20260803.txt)
  and [repeatability pass](../../experiments/2026-08-03-a72-cpu8-late-hold/results/runtime-attempt-2-repeatability-pass-20260803.txt)
- [CPU8/CPU9 scheduler-context execution](../../experiments/2026-08-03-a72-scheduler-context/README.md)
  with [first unpark pass](../../experiments/2026-08-03-a72-scheduler-context/results/runtime-unpark-attempt-1-pass-20260805.txt)
  and [repeatability pass](../../experiments/2026-08-03-a72-scheduler-context/results/runtime-unpark-attempt-2-repeatability-pass-20260805.txt)
- [Post-event identification lifecycle](../../experiments/2026-08-01-da921x-post-event-lifecycle/README.md)
  and [sanitized runtime result](../../experiments/2026-08-01-da921x-post-event-lifecycle/results/runtime.txt)
- [Netlink skb serialization runtime](../../experiments/2026-07-31-da921x-netlink-skb-serialization/README.md)
  and [sanitized runtime result](../../experiments/2026-07-31-da921x-netlink-skb-serialization/results/runtime.txt)
- [Corrected OF event-layout runtime](../../experiments/2026-07-31-da921x-of-event-layout-correction/README.md)
  and [sanitized runtime result](../../experiments/2026-07-31-da921x-of-event-layout-correction/results/runtime.txt)
- [Legacy-family driver and binding contract](../../experiments/2026-07-29-da921x-legacy-driver-contract/README.md)
- [Exact read-only board-contract experiment](../../experiments/2026-07-28-da9214-gauss/README.md)
  and [sanitized runtime result](../../experiments/2026-07-28-da9214-gauss/results/runtime-candidate-gauss-attempt-1-20260728.txt)
- [DA921x/I2C6/A72 experiment line](../../experiments/README.md#current-da921x-i2c6-and-a72-line),
  including the rejected and superseded discriminators
- [Name-only client discriminator](../../experiments/2026-07-30-da921x-name-only-client/README.md)
  and [attempt 2 runtime result](../../experiments/2026-07-30-da921x-name-only-client/results/runtime-name-only-attempt-2-20260730.txt)
- [OF-modalias isolation](../../experiments/2026-07-30-da921x-of-modalias-isolation/README.md)
  and [runtime result](../../experiments/2026-07-30-da921x-of-modalias-isolation/results/runtime-attempt-1-20260730.txt)
- [Gemian DA9214 direct-address reconciliation](../../experiments/2026-07-27-da9214-direct-address-cassini/results/gemian-da9214-live-dump-reconciliation-20260727.txt)
- [DA9214 resource-only mismatch](../../experiments/2026-07-23-da9214-resource-only/README.md)
- [A72 firmware/power contract](../../experiments/2026-07-22-a72-firmware-power-contract/README.md)
- [Gemian A72 read-only discovery](../../experiments/2026-07-22-gemian-a72-readonly-discovery/README.md)
