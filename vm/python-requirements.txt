# Keep the top-level Device Tree schema tool version explicit. Provisioning
# records the fully resolved Python environment inside the guest.
dtschema==2026.6
# Reconstruct symbol-bearing ELFs from raw/compressed Linux kernel images.
vmlinux-to-elf==1.3.6
